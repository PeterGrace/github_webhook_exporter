apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: unsupported-api
spec:
  rules:
    - host: unsupported.example.com
      http:
        paths:
          - path: /
            backend:
              serviceName: unsupported
              servicePort: 80
