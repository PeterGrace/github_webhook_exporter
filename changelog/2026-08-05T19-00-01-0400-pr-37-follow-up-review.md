# PR 37 follow-up review

## Changes

- Clarified that `GHE_OTEL_SHUTDOWN_TIMEOUT_SECONDS` is validated but remains unenforced until issue
  #36 implements bounded provider shutdown.
- Documented that explicitly empty signal-specific OTLP headers clear inherited generic headers,
  while absent signal-specific settings inherit them.
- Marked the blocked-exporter saturation test as the regression check for the OpenTelemetry 0.32
  queue-occupancy invariant.

## Follow-up disposition

- Issue #35 already requires direct non-recursive stderr diagnostics limited to one report per
  signal/reason category per minute, alongside Prometheus counters.
- Issue #36 owns revalidation of queue accounting under final provider shutdown ordering.
