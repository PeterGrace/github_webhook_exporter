# PR 28 review response

- Replaced the free queue outcome/reason parameters with `MergeQueueCompletion`, whose evidence-backed variants map to exactly one supported label pair.
- Kept failed and cancelled outcomes reserved and unreachable through the Phase 3 update API until a future classifier adds matching reason vocabulary.
- Reduced startup seeding to the two completion pairs Phase 3 can emit.
- Preserved semantic webhook dispatch as explicit follow-up scope from issue 23.
- Confirmed Prometheus' `le="1"` bucket already includes valid sub-second observations.
