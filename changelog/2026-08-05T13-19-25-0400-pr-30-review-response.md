# PR 30 Review Response

## Clarified

- Documented that receipt-time queue timestamp formatting failure is a defensive-only branch because
  production receipt times come from `OffsetDateTime::now_utc()`.
- Added a debug assertion so a future synthetic, out-of-range receipt timestamp is visible during
  development without changing the production webhook response contract.
- Documented the durable canonical-timestamp invariant used when completion returns and validates
  an attempt's persisted enqueue timestamp.
- Confirmed that invalid durations intentionally update only `invalid_duration`, as established by
  issue #23 and verified by signed-router coverage; durable outcomes with invalid durations do not
  update outcome or duration metrics.
