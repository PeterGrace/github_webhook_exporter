# PR 41 review response

- Serialized timeout finalization with exporter batch claims so records counted as
  `pipeline_closed` cannot later be exported by a detached shutdown worker.
- Moved blocking provider shutdown orchestration onto Tokio's blocking pool while preserving the
  original service result.
- Replaced the process test's post-hoc wall-clock assertion with an active child-process deadline,
  used an unbindable loopback port, and accepted any normalized bounded exporter failure reason.
- Added queue-ordering regressions for both timeout-finalization-first and export-claim-first races.
