# PR 49 review response

- Tightened `.dockerignore` so future files under `.cargo/` remain excluded while
  `.cargo/config.toml` stays available to the build.
- Exercised the application's default IPv6 wildcard bind address instead of overriding it in the
  container smoke flow.
- Verified UID/GID 65532 ownership for the live SQLite database and its WAL/SHM sidecars before
  graceful shutdown.
