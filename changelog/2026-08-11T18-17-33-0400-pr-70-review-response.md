# PR 70 review response

Documented why the blocked-collector webhook test expects zero OpenTelemetry log exports: the
generic completion event is local-only and the successful workflow path emits no other logs. The
comment also makes explicit that a future remotely eligible webhook log should break this invariant.

## Validation

- `cargo test telemetry::otlp_test::blocked_collector_does_not_change_completed_workflow_response --lib -- --exact`
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets -- -D warnings`
