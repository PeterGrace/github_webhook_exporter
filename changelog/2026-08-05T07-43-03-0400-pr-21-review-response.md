# PR 21 review response

- Preserve the Axum server I/O error when both server drain and background task joining fail.
- Configure the retention interval to skip missed ticks instead of immediately replaying stale
  ticks after a long backlog pass.
- Add direct tests for invalid retention configuration and redacted, correlated prune failures.
- Document that repository gauge mutations occur after SQLite commits and startup reconciles any
  value lost to a forced-exit window.
