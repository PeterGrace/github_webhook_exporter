# PR 18 review response

- Clarified in the implementation plan that the prune batch constant is signed because SQLite `LIMIT` parameters use signed integers, while affected-row counts remain `u64`.
- Replaced SQLite floating-point cutoff conversion with fixed-width millisecond UTC formatting in Rust before the indexed retention query.
- Preserved and reran fractional-second retention coverage to verify exact cutoff behavior.
- Clarified the pull request description that claim and prune scheduling are staged infrastructure and remain intentionally unwired in this issue.
