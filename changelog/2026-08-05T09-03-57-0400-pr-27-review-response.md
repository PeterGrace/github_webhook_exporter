# PR 27 review response

- Documented that completed-attempt pruning relies on the fixed-width UTC millisecond timestamp
  invariant enforced by `QueueTimestamp`.
- Reopened the merge-queue lock test with a 10-millisecond busy timeout so it verifies redacted
  unavailable-error mapping without adding five seconds to the test suite.
- Retained the explicit enqueue transaction because issue 22 requires all queue state transitions
  to execute transactionally; the partial unique index still provides the active-attempt invariant.
