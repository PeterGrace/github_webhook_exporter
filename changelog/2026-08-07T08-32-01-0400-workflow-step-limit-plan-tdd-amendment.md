# Workflow Step Limit Plan TDD Amendment

- Moved duplicate-rejection coverage before webhook enforcement so it fails for missing production behavior rather than an intentionally incorrect assertion.
