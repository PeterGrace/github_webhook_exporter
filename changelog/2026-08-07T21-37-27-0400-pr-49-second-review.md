# PR 49 second review response

- Kept the data directory and main SQLite database as mandatory ownership-audit paths.
- Made WAL and SHM sidecar ownership checks conditional on each file being present, avoiding a
  permanent contract with SQLite checkpoint internals while still validating live sidecars.
- Retained the digest-pinned BusyBox ownership helper as a smoke-test-only dependency.
