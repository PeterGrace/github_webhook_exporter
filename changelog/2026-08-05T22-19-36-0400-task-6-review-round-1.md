# Task 6 Review Round 1: Integrated Failure and Retention Evidence

## Summary

- Added a real queue-state persistence failure to the integrated OTLP privacy matrix by rejecting a
  pull-request queue completion in SQLite.
- Asserted the exact bounded `operation.failure` event name, sole `ghe.failure.reason` key,
  `queue_state` value, failed operation outcome, and error span status.
- Added the queue failure delivery, pull-request number, and raw store detail to the cross-surface
  privacy checks.
- Strengthened retention hierarchy coverage to require exactly the direct successful
  `delivery.prune` and `merge_queue.prune` `sqlite.query` children.
- Asserted that the seeded retention delivery and retention pull-request row identifier are absent
  from trace protobuf, log protobuf, stderr, and Prometheus exposition.
- Added ordered fixture finalization so failure-path spans close before the OTLP providers flush.

## TDD Evidence

The new queue-failure lookup initially failed because the integrated test emitted no corresponding
request or `operation.failure` event. After adding the persistence failure, repeated runs exposed an
OTLP capture lifecycle race. Explicitly dropping request-serving fixture resources before the final
provider flush eliminated the race without changing production instrumentation.

The final integrated test passed 40 consecutive runs after the lifecycle correction, including 10
runs after formatting.

## Validation

The required validation sequence passed in order:

1. `just fmt`
2. `cargo build`
3. `cargo clippy --all-targets -- -D warnings`
4. `just test` (172 passed, 0 failed)
5. `cargo doc --no-deps`

The affected test also passed with:

```text
cargo test telemetry::otlp_test::integrated_core_trace_privacy --lib -- --nocapture
```
