# PR 37 review response

## Changes

- Replaced the standalone OTLP integration test's process-global environment mutation with a
  crate-local test that injects `TelemetryConfig` values through the existing lookup boundary.
- Strengthened OTLP pipeline coverage with blocked trace and log exporters, deterministic queue
  saturation, exact accepted/drop/pending assertions, and one-record protobuf batch verification.
- Documented the admission/SDK queue occupancy invariant: SDK occupancy cannot exceed the
  application `pending` count, so equal capacities cause the application boundary to reject before
  the SDK queue can overflow.
- Documented that OpenTelemetry 0.32 has no public constant-time `LogBatch` length accessor.
- Clarified the interim operational limitation that drops and export failures remain invisible until
  issue #35 connects the existing hooks to Prometheus and direct non-recursive stderr diagnostics.

## Review disposition

- Operator-visible drop and failure reporting remains intentionally scoped to issue #35.
- The suggested SDK-side slot leak cannot occur while the documented occupancy and producer-before-
  shutdown invariants hold; the deterministic blocked-exporter test now exercises this saturation
  case and proves pending returns to zero after flush.
