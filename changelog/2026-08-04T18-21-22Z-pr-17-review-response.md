# PR 17 review response

- Documented that webhook authentication error values are uniform while execution time is not equalized.
- Required future HTTP callers to keep unauthorized responses and logging identical so they do not widen the coarse repository timing signal.
- Clarified that HMAC-SHA-256 accepts arbitrary key lengths and retains defensive fail-closed handling for its fallible constructor API.
