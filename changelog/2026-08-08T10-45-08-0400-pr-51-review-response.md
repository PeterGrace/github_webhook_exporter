# PR #51 review response

- Quoted the schema-constrained image pull policy and Service type in rendered YAML so future enum
  expansion cannot accidentally change their scalar types.
- Confirmed optional OTLP header references remain fail-closed once an operator configures a Secret
  key name; silently omitting configured authentication headers would hide deployment errors.
- Retained the application-aligned webhook body limit and existing DNS-safe Kind suffix generation.
