# PR 20 review response

- Preserved the specification's fixed webhook result vocabulary instead of adding an unapproved
  `internal` metric label.
- Made unexpected webhook response statuses visible through a fixed-field error log containing
  only the numeric status and normalized `unavailable` fallback result.
- Added a debug assertion so future middleware changes that emit an undocumented status fail
  immediately during development and tests.
- Added a regression test proving unexpected statuses trigger the debug assertion.
