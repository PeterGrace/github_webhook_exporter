# PR 9 review response

- Added a full-router regression test proving JSON request bodies above Axum's default body limit are normalized to the stable `400 invalid_request` API contract.
- Retained the API-level empty PATCH guard because it rejects invalid requests before opening a storage transaction; the store-level guard remains a domain-boundary invariant.
