# PR 39 Review Response

- Removed module-wide dead-code allowances from the central trace and workflow telemetry modules.
- Limited test-only pull-request collection helpers with `#[cfg(test)]` so production dead-code
  checking remains active.
- Preserved the explicit issue requirement to emit every reported workflow step; admission remains
  bounded by the validated 2 MiB maximum webhook body size and the existing bounded OTLP queue.
