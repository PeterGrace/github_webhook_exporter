# Secure exposure and network boundaries design

- Defined independent opt-in webhook, metrics, and administrative Helm exposure resources.
- Defined selector-bounded default-deny ingress and egress NetworkPolicy behavior.
- Recorded the shared-listener HTTP path limitation and supported L7 enforcement choices.
