# PR 11 review response

- Added a normalized warning when a SQLite readiness probe fails without exposing database details.
- Added log-capture coverage for the readiness warning and empty `503` response.
- Documented the controlled-time scheduling invariant in the shutdown timeout test.
- Removed the unused unbounded server entry point so production and tests share the bounded
  lifecycle path.
- Deduplicated the in-flight drain test setup.
