# Task 3 fix round 1

## Summary
- tightened `scripts/github-actions-test.sh` to require `RUSTUP_TOOLCHAIN=1.97.1` in the validation job env
- tightened the reproducibility-script contract to require `--no-cache` and exactly two `build_image` calls
- kept the fix scoped to contract tests; workflow and reproducibility implementation files were unchanged

## Verification
- mutation probe: workflow toolchain pin drift now fails with `workflow must pin RUSTUP_TOOLCHAIN=1.97.1`
- mutation probe: removing `--no-cache` now fails with `image reproducibility test must disable cache with --no-cache`
- mutation probe: removing the second `build_image` call now fails with `image reproducibility test must call build_image exactly twice`
- `just workflow-test`
